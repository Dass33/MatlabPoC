# Configuration Tab

The **Configuration** tab handles the physical parameters of the Nanofluidic Scattering Microscopy (NSM) experiment.

A **Load config** expander lets users apply a saved preset or an uploaded config JSON file, and a
**Save preset / config** expander lets them export the current settings or save them as a named
preset for reuse.

---

## Code Reference

::: config
