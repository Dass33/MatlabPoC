# Dev-only override, live-edits Python without rebuilding the image
  streamlit:
    volumes:
      - ./streamlit:/app
    environment:
      STREAMLIT_SERVER_RUN_ON_SAVE: "true"
      STREAMLIT_SERVER_FILE_WATCHER_TYPE: poll
